This project explores the Titanic dataset with EDA and Feature Engineering.
Regression models are applied to predict Fare values.
Model and scaler are saved using joblib for reuse in Streamlit UI.
Streamlit app allows interactive inputs and displays predictions.
EDA graphs (Survival, Class, Age, Fare) are visualized for insights.
Requirements are listed in requirements.txt for easy setup.
